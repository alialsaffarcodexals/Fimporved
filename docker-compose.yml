services:
  forum:
    build: .
    image: forum-mvp:latest
    container_name: forum-mvp
    ports:
      - "8080:8080"
    volumes:
      - forum_data:/data   # mount at /data (writable by app user)
    restart: unless-stopped

volumes:
  forum_data: {}
